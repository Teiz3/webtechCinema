//- extends layout

block content
  h1 Whoops, it seems like there was an error!
  h1= message
  h2= error.status
  //- pre #{error.stack} commented out in final version to not reveal to much info to the user
